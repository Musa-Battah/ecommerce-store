/** @type {import('next').NextConfig} */
const nextConfig = {
  allowedDevOrigins: ['preachy-unshaken-spotlight.ngrok-free.dev'],
  // For production, also add your domain
  // If deploying to Vercel, this is automatically handled
};

module.exports = nextConfig;